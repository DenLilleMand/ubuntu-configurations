see  :help clipboard inside of neovim, it needs a clipboard provider on the path in order to work, 
i installed xsel "sudo apt-get install xsel" which is a newer version of xclip(the most popular i think)
