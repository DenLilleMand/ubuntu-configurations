Seems like a too popular (terminal) to miss out on:
http://www.zsh.org/
https://github.com/robbyrussell/oh-my-zsh

can just install it with:

sudo apt-get install zsh

It fucked my nice solarized light theme in the console though, so i currently dont see a reason to change.
